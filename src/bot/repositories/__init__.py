"""
Data Access Layer (Repositories).

Implements the Repository pattern to encapsulate database queries
and abstract the SQLAlchemy ORM layer away from the business logic.
All services should use repositories instead of raw sessions.
"""
