"""
Structured Logging Configuration.

Configures structlog to format logs as JSON in production
and as colored human-readable text in development. Includes
custom processors for injecting context (user_id, group_id).
"""
